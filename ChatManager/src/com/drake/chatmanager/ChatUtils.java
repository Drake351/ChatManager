package com.drake.chatmanager;

import org.bukkit.ChatColor;

public class ChatUtils {
static String prefixchat = ChatColor.GRAY + "[" + ChatColor.DARK_RED + "Chat" + ChatColor.GRAY + "] " + ChatColor.RED;
	
	public static String prefixChat(){
		return prefixchat;
	}
}
